<% unless session[:user_id].nil? %>
<ul class="nav nav-pills" >
  <li>
    <%= image_tag("header.png", { :class => "logo" }) %>
  </li>
  <li>
    <%=link_to "Pipeliner", "dashboard#index" %>
  </li>
</ul>

<% unless @model.nil? %>
<ul class="nav nav-pills">
  <li>
    <%= link_to "Create a new Revision", "/model/revise/#{@model.id}" %>
  </li>
</ul>
<%end%>
  

  <div class="pull-right" id="login">
    <em class="pull-left"><%= User.find(session[:user_id]).name %></em>
    <img src="<%= User.find(session[:user_id]).gravatar_url(50) %>" class="img-rounded pull-right"/>
  </div>

<% end %>